AWSTemplateFormatVersion: '2010-09-09'
Description: CloudFormation template for API Gateway with Cognito authentication, Lambda integrations, IAM permissions, and existing S3 bucket storing Lambda code with versioning
Parameters:
  S3BucketName:
    Type: String
    Description: Name of the existing S3 bucket storing Lambda code
  S3KeyPrefix:
    Type: String
    Description: Folder path in the existing S3 bucket where Lambda code is stored
  DynamoDBTableName:
    Type: String
    Description: Name of the existing DynamoDB table
  CognitoUserPoolId:
    Type: String
    Description: ID of the Cognito User Pool for API access restriction
  Stage:
    Type: String
    Description: Deployment stage for the API (e.g., dev, prod)
Resources:
  # S3Bucket:
  #   Type: AWS::S3::Bucket
  #   Properties:
  #     BucketName: !Ref S3BucketName
  #     VersioningConfiguration:
  #       Status: Enabled
  LambdaExecutionRole:
    Type: AWS::IAM::Role
    Properties:
      RoleName: LambdaExecutionRole
      AssumeRolePolicyDocument:
        Version: '2012-10-17'
        Statement:
          - Effect: Allow
            Principal:
              Service: lambda.amazonaws.com
            Action: sts:AssumeRole
      Policies:
        - PolicyName: LambdaDynamoS3Policy
          PolicyDocument:
            Version: '2012-10-17'
            Statement:
              - Effect: Allow
                Action:
                  - dynamodb:*
                  - s3:*
                Resource: '*'
  AddClientLambda:
    Type: AWS::Lambda::Function
    Properties:
      FunctionName: AddClientFunction
      Handler: index.handler
      Role: !GetAtt LambdaExecutionRole.Arn
      Runtime: nodejs18.x
      Environment:
        Variables:
          DYNAMODB_TABLE_NAME: !Ref DynamoDBTableName
      Code:
        S3Bucket: !Ref S3BucketName
        S3Key: !Sub "${S3KeyPrefix}/addClient.zip"
        S3ObjectVersion: "khMqEMEP2d6QnU6pYfifrU9Y9vDK6MjL"
  DeleteClientLambda:
    Type: AWS::Lambda::Function
    Properties:
      FunctionName: DeleteClientFunction
      Handler: index.handler
      Role: !GetAtt LambdaExecutionRole.Arn
      Runtime: nodejs18.x
      Environment:
        Variables:
          DYNAMODB_TABLE_NAME: !Ref DynamoDBTableName
      Code:
        S3Bucket: !Ref S3BucketName
        S3Key: !Sub "${S3KeyPrefix}/deleteClient.zip"
        S3ObjectVersion: "r0By4a258lwfWJV1haN81AmXlee0aRkz"
  GetClientLambda:
    Type: AWS::Lambda::Function
    Properties:
      FunctionName: GetClientFunction
      Handler: index.handler
      Role: !GetAtt LambdaExecutionRole.Arn
      Runtime: nodejs18.x
      Environment:
        Variables:
          DYNAMODB_TABLE_NAME: !Ref DynamoDBTableName
      Code:
        S3Bucket: !Ref S3BucketName
        S3Key: !Sub "${S3KeyPrefix}/getClient.zip"
        S3ObjectVersion: "Z.GpLec47hSPmRpYslVlZI91KZvy_HTS"
  ApiGateway:
    Type: AWS::ApiGateway::RestApi
    Properties:
      Name: ClientApi
  CognitoAuthorizer:
    Type: AWS::ApiGateway::Authorizer
    Properties:
      Name: CognitoAuthorizer
      Type: COGNITO_USER_POOLS
      RestApiId: !Ref ApiGateway
      IdentitySource: method.request.header.Authorization
      ProviderARNs:
        - !Sub arn:aws:cognito-idp:${AWS::Region}:${AWS::AccountId}:userpool/${CognitoUserPoolId}
  ApiResourceManageClients:
    Type: AWS::ApiGateway::Resource
    Properties:
      ParentId: !GetAtt ApiGateway.RootResourceId
      PathPart: manageclients
      RestApiId: !Ref ApiGateway
  ApiDeployment:
    Type: AWS::ApiGateway::Deployment
    DependsOn:
      - AddClientMethod
      - DeleteClientMethod
      - GetClientMethod
    Properties:
      RestApiId: !Ref ApiGateway
      StageName: !Ref Stage
  AddClientMethod:
    Type: AWS::ApiGateway::Method
    Properties:
      RestApiId: !Ref ApiGateway
      ResourceId: !Ref ApiResourceManageClients
      HttpMethod: POST
      AuthorizationType: NONE
      # AuthorizationType: COGNITO_USER_POOLS
      # AuthorizerId: !Ref CognitoAuthorizer
      Integration:
        IntegrationHttpMethod: POST
        Type: AWS_PROXY
        Uri: !Sub arn:aws:apigateway:${AWS::Region}:lambda:path/2015-03-31/functions/${AddClientLambda.Arn}/invocations
  DeleteClientMethod:
    Type: AWS::ApiGateway::Method
    Properties:
      RestApiId: !Ref ApiGateway
      ResourceId: !Ref ApiResourceManageClients
      HttpMethod: DELETE
      AuthorizationType: NONE
      # AuthorizationType: COGNITO_USER_POOLS
      # AuthorizerId: !Ref CognitoAuthorizer
      Integration:
        IntegrationHttpMethod: POST
        Type: AWS_PROXY
        Uri: !Sub arn:aws:apigateway:${AWS::Region}:lambda:path/2015-03-31/functions/${DeleteClientLambda.Arn}/invocations
  GetClientMethod:
    Type: AWS::ApiGateway::Method
    Properties:
      RestApiId: !Ref ApiGateway
      ResourceId: !Ref ApiResourceManageClients
      HttpMethod: GET
      AuthorizationType: NONE
      # AuthorizationType: COGNITO_USER_POOLS
      # AuthorizerId: !Ref CognitoAuthorizer
      Integration:
        IntegrationHttpMethod: POST
        Type: AWS_PROXY
        Uri: !Sub arn:aws:apigateway:${AWS::Region}:lambda:path/2015-03-31/functions/${GetClientLambda.Arn}/invocations
  OptionsMethod:
    Type: AWS::ApiGateway::Method
    Properties:
      AuthorizationType: NONE
      HttpMethod: OPTIONS
      ResourceId: !Ref ApiResourceManageClients
      RestApiId: !Ref ApiGateway
      Integration:
        IntegrationHttpMethod: OPTIONS
        Type: MOCK
        PassthroughBehavior: WHEN_NO_MATCH
        RequestTemplates:
          application/json: '{"statusCode": 200}'
      MethodResponses:
        - StatusCode: 200
          ResponseParameters:
            method.response.header.Access-Control-Allow-Origin: "'*'"
            method.response.header.Access-Control-Allow-Methods: "'GET, POST, DELETE, OPTIONS'"
            method.response.header.Access-Control-Allow-Headers: "'Content-Type, Authorization'"
  LambdaInvokePermissionAdd:
    Type: AWS::Lambda::Permission
    Properties:
      FunctionName: !GetAtt AddClientLambda.Arn
      Action: lambda:InvokeFunction
      Principal: apigateway.amazonaws.com
  LambdaInvokePermissionDelete:
    Type: AWS::Lambda::Permission
    Properties:
      FunctionName: !GetAtt DeleteClientLambda.Arn
      Action: lambda:InvokeFunction
      Principal: apigateway.amazonaws.com
  LambdaInvokePermissionGet:
    Type: AWS::Lambda::Permission
    Properties:
      FunctionName: !GetAtt GetClientLambda.Arn
      Action: lambda:InvokeFunction
      Principal: apigateway.amazonaws.com
Outputs:
  ApiGatewayEndpoint:
    Description: "Endpoint URL for Client API"
    Value: !Sub "https://${ApiGateway}.execute-api.${AWS::Region}.amazonaws.com/prod/manageclients"
  S3BucketName:
    Description: "S3 Bucket storing Lambda code"
    Value: !Ref S3BucketName
  DynamoDBTableName:
    Description: "DynamoDB table used by Lambda functions"
    Value: !Ref DynamoDBTableName
